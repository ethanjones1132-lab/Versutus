# Hands-free Background Voice Chat Implementation Plan

**Goal:** Add a user-started, half-duplex voice-call mode that endpoints speech, sends the finished utterance automatically, speaks the completed reply, and listens for the next turn while the app is backgrounded or the screen is locked.

**Architecture:** A local Expo module owns the native microphone/audio session and Android foreground service for the lifetime of a call. A root React provider runs a deterministic turn state machine and sends recognized text through the existing `sendChatInput` path; the Gate and every backend remain unchanged. Push-to-talk continues to use `expo-speech-recognition` -> composer draft -> explicit Send, and ordinary optional playback continues to use `expo-speech`.

**Tech stack:** Expo SDK 57 / React Native 0.86, Expo Modules API, Android `SpeechRecognizer` + `TextToSpeech` + microphone/media-playback foreground service, iOS `SFSpeechRecognizer` + `AVAudioEngine` + `AVSpeechSynthesizer`, existing Gateway context and Jest suite.

**Spec:** `FUTURE-ITEMS.md`, “Hands-free background voice chat” and “Solution B — Voice chat capability (B1–B3 SHIPPED)”.

## Decision

Choose **(a) client-side turn-taking**.

It is the only route that immediately covers every connected Hermes, OpenCode, Codex, and Claude Code profile through the same existing text-chat contract. Audio stays on the operator’s phone; only the recognized text and the existing text response cross the already-paired connection. The app gains no model credential, the Gate gains no provider credential or audio broker, and there is no paid realtime-provider dependency.

This is deliberately a **half-duplex call**, not Solution B4. While the operator is speaking, reply playback is stopped; while the reply is streaming or being spoken, full recognition is not producing a user turn. The implementation forfeits B4's low-latency audio-to-audio stream, provider-side semantic endpointing, and continuous simultaneous listen-while-speaking. Those losses are accepted for the backend coverage, credential posture, and smaller Gate-free scope.

**Revised 2026-09-12 against current industry practice.** OpenAI's July 2026 "GPT-Live" is genuinely full-duplex — it listens and speaks at once and can murmur backchannel acknowledgments while the user is still talking. That is not being matched here; it requires continuous bidirectional audio streaming and is exactly what B4 (and now more than B4 originally scoped) would take. What *is* in reach without that architecture, and folded into the steps below: **interrupt-by-speaking**. Google's own Gemini Live API does not pause its TTS server-side either — the documented integration pattern is client-side voice-activity detection that stops playback locally. That is the same shape of fix applied here: a lightweight on-device VAD listens during `speaking` (reusing the communication-mode audio session Step 2/3 already configure for echo cancellation) and an interrupt hands control back to listening immediately. It recovers "just start talking to interrupt," not simultaneous listen-and-speak or verbal backchanneling — the plan must not describe it as full-duplex or as parity with GPT-Live.

The call path deliberately diverges from B1’s “never auto-send” composer rule. The modes are visibly and structurally separate:

- The existing microphone remains hold-to-talk dictation. Its transcript continues to land in the composer for review and never sends. (Corrected 2026-09-12 second review: the control's actual accessibility label is "Hold to talk", `chat-composer.tsx:430`; "Hold to dictate" appears nowhere in the code.)
- A separate phone control is **Start hands-free call**. Before the first call, a sheet says “Speech is sent automatically when you pause” and requires an explicit **Start call** tap.
- During a call, a persistent banner says **Hands-free call · speech auto-sends** and exposes Mute, Skip reply, and End. Call transcripts never pass through the composer writer.
- Ending the call restores the untouched typed draft. Any recognized text that was not accepted by the connected send path is recovered into that draft for review, never queued for later automatic delivery.

## Seamlessness pass (2026-09-12, informed by current voice-assistant practice)

Four additions to the steps below, made after checking current documented
behavior of the major voice assistants rather than assuming it. None of them
reopen the (a)/(b) decision above or add a backend/provider dependency — each
is a client-side addition to the architecture already chosen.

1. **Interrupt-by-speaking (barge-in via on-device VAD).** Documented as the
   standard integration pattern for exactly this situation: Google's own
   Gemini Live API does not pause TTS server-side when the user speaks —
   the API documentation directs client integrators to implement client-side
   audio mixing or silence detection to suppress agent audio. That is what is
   added here: a lightweight voice-activity listener, active only during
   `speaking`, reusing the communication-mode audio session Steps 2–3 already
   configure (which already does hardware/software echo cancellation for this
   exact reason). It does not run full recognition and does not transcribe —
   it only detects that the user started talking, stops playback, and hands
   off to a real listening turn. See Step 3 (`bargeIn` event) and Step 4
   (`speaking -> listening` on `bargeIn`).
2. **A grace window before a silence-triggered send.** OpenAI's original
   turn-based Advanced Voice Mode's most-cited failure was exactly this: it
   detected the user's turn by listening for silence, so a pause to think
   was frequently mistaken for "done talking" and the model barged in with an
   unwanted answer. This plan's fixed silence thresholds (Step 3) are exposed
   to the identical failure mode. Rather than guess at different fixed
   milliseconds without device data, Step 4 adds a short client-side grace
   hold after a silence-triggered `final`: if the user resumes speaking before
   the hold elapses, the pending send is cancelled and the utterance
   continues. This is a pure state-machine change, consistent with the
   existing reducer's tested handling of duplicate/late native events.
3. **Progressive sentence-level speech.** Well-established latency technique
   (search results below cite the general "speak while the model is still
   generating" pattern, sometimes called LLM2Speech): synthesizing and
   playing audio for a completed sentence while later sentences are still
   streaming in, rather than waiting for the full reply to finish. B2 already
   implements sentence-boundary chunking for ordinary playback
   (`Speech.maxSpeechInputLength`) — Step 6 reuses that exact chunking during
   a call instead of the full-message wait it currently requires.
4. **Ambient state feedback instead of banner text alone.** Both major voice
   assistants use a continuously animated, audio-reactive visual (a
   pulsing orb/waveform) rather than a static status label, because it signals
   "it is listening to you right now" far more convincingly than text. Step 7
   adds a lightweight amplitude-driven animation using the Skia dependency
   already in this repo, plus a short non-verbal earcon the instant a send is
   dispatched — an honest analog to GPT-Live's verbal backchannel murmurs
   ("mmhm" while you talk), which cannot be reproduced here without either
   fabricating speech the model never produced or true full-duplex audio.

**What remains forfeited even after this pass**, stated plainly so it is never
mistaken for parity: continuous simultaneous listen-while-speaking, verbal
backchanneling while the user talks, and provider-side semantic
understanding of *when* a thought is actually finished. Those require true
full-duplex audio streaming — Solution B4 or beyond — and are out of scope
here for the same credential/cost reasons the Decision section already gives.

Sources consulted: [I Talked to ChatGPT's GPT-Live for a Week](https://medium.com/@ffguci8/i-talked-to-chatgpts-gpt-live-for-a-week-here-s-why-it-changes-everything-0f89ade4d3c6), [ChatGPT Voice vs Grok vs Gemini Live (2026)](https://tech-insider.org/chatgpt-voice-vs-grok-voice-vs-gemini-live-2026/), [Speak While You Think: Streaming Speech Synthesis During Text Generation](https://arxiv.org/pdf/2309.11210).

## Global constraints and compatibility facts

- B5 remains absolute: there is no wake word, boot receiver, background start, or automatic resume. Android starts its microphone service only from the visible Start-call tap; both platforms stop on End, system interruption, disconnect, or process death.
- No code may branch on `hermes`, `opencode`, `codex`, or `claude-code`. Call availability is the conjunction of a connected text-chat surface and advertised/on-device audio readiness. No `realtime-voice` capability is required or invented.
- `expo-speech-recognition@57.0.0` and `expo-speech@57.0.2` remain installed for B1–B3 and are not refactored into the call runtime. Their foreground behavior is shipped; their sustained background behavior is not documented, so the call does not assume it.
- Do not add `expo-audio` as a keepalive. The [SDK 57 audio docs](https://docs.expo.dev/versions/v57.0.0/sdk/audio/) confirm its background service belongs to its own recorder/player; running a second recorder beside speech recognition would contend for the microphone. The local module uses the SDK 57 Expo Modules API and the platform speech/audio APIs directly.
- Exact native configuration is locked. Android declares `RECORD_AUDIO`, `POST_NOTIFICATIONS`, `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MICROPHONE`, and `FOREGROUND_SERVICE_MEDIA_PLAYBACK`, plus a non-exported service with `android:foregroundServiceType="microphone|mediaPlayback"` and `android:stopWithTask="true"`. iOS declares `UIBackgroundModes = ["audio"]` and keeps the existing `NSMicrophoneUsageDescription` and `NSSpeechRecognitionUsageDescription` strings accurate for both dictation and calls.
- Do not use Android’s `phoneCall` foreground-service type, Telecom/ConnectionService, or iOS CallKit: this is a model conversation, not a carrier/VoIP peer call. Use a microphone/media-playback service and an ordinary ongoing notification.
- The device acceptance gate in Final verification items 3–8 is mandatory. Expo Modules API compatibility with SDK 57 is documented, but native background recognition across OEMs is not confirmed until the physical-device matrix passes. A failure blocks release; it must not be relabeled as a foreground-only success.

## Ordered implementation steps

### 1. Scaffold the local native boundary and pin generated configuration

**Files:** create `modules/handsfree-voice/expo-module.config.json`, `modules/handsfree-voice/index.ts`, `modules/handsfree-voice/src/HandsfreeVoiceModule.ts`, `modules/handsfree-voice/android/build.gradle`, `modules/handsfree-voice/ios/HandsfreeVoice.podspec`; modify `app.json`, `scripts/verify-config.mts`.

- [ ] Run `npx create-expo-module@latest handsfree-voice --local --name HandsfreeVoice --package com.versutus.handsfreevoice --platform apple android --features AsyncFunction Event --barrel`. Keep it local under the default `modules/` autolinking directory; add no standalone package or example app.
- [ ] Replace generated sample exports with a typed native contract: `getAvailability(): Promise<{ recognition: boolean; synthesis: boolean; maxSpeechInputLength: number }>`, `startSession({ title }): Promise<'started'|'permission-denied'|'unavailable'>`, `startListening(): Promise<boolean>`, `stopListening(): Promise<void>`, `speak({ chunks, voiceIdentifier?, rate?, pitch? }): Promise<boolean>`, `stopSpeaking(): Promise<void>`, `setMuted(boolean): Promise<void>`, `playSendEarcon(): Promise<void>` (plays the bundled send earcon through the call's own audio session — Step 7's earcon has no other playback path: `expo-audio` is excluded by the global constraints and `expo-speech` is speech-only), and `stopSession(): Promise<void>`. Events are `partial`, `final`, `noSpeech`, `speechFinished`, `interruption`, `endRequested` (Android only — iOS has no ongoing notification with an End action; see Step 2), `fatalError`, `bargeIn` (voice detected during `speaking`; see the seamlessness pass above), and `level` (periodic amplitude 0–1, for the ambient visual in Step 7 — not required for correctness, drop silently if a platform cannot supply it cheaply). (Scaffold command verified 2026-09-12 against the [create-expo-module docs](https://docs.expo.dev/more/create-expo-module/): `--local`, `--name`, `--package`, `--platform apple android`, `--features AsyncFunction Event`, and `--barrel` are all real options.)
- [ ] In `app.json`, add the five Android permissions named above; add `ios.infoPlist.UIBackgroundModes: ["audio"]`; revise the existing speech-recognition permission strings to say the mic/recognizer support message dictation and a hands-free call the user starts.
- [ ] Extend `scripts/verify-config.mts` to assert those exact permissions/background mode and reject `phoneCall`, a boot receiver, or a sticky-service declaration.

**Verify:** `npx expo config --type public`, `npm run verify:config`, and `npx expo-doctor` all pass. The task’s diff adds no dependency to `package.json` or `package-lock.json` and contains no `expo-audio` entry: this step adopts no runtime library.

### 2. Implement the Android session service and user-visible lifetime

**Files:** create `modules/handsfree-voice/android/src/main/AndroidManifest.xml`, `modules/handsfree-voice/android/src/main/java/com/versutus/handsfreevoice/HandsfreeVoiceModule.kt`, `modules/handsfree-voice/android/src/main/java/com/versutus/handsfreevoice/HandsfreeCallService.kt`, `modules/handsfree-voice/android/src/main/java/com/versutus/handsfreevoice/HandsfreeCallState.kt`, `modules/handsfree-voice/android/src/test/java/com/versutus/handsfreevoice/HandsfreeCallStateTest.kt`.

- [ ] Merge the five permissions and declare `HandsfreeCallService` as `exported=false`, `foregroundServiceType="microphone|mediaPlayback"`, `stopWithTask=true`. Return `START_NOT_STICKY`; `onTaskRemoved` and `onDestroy` must release recognition, TTS, audio focus, and the notification before `stopSelf()`.
- [ ] Start with `ContextCompat.startForegroundService` only while the Activity is resumed and microphone permission is granted. Promote immediately with a stable `handsfree-call` channel, an ongoing “Hands-free call with {title}” notification, microphone-use copy, and an **End call** action. The action must do both halves of End: emit `endRequested` to JS (when the runtime is alive) so the Step 4 reducer reaches its terminal phase, promotes recovery, and dismisses the banner, and then run the service's own native teardown. "Handled by the service itself" alone leaves the JS state machine stuck mid-call with the banner up — the event is declared in Step 1's contract and consumed in Steps 4/6, so it must actually be sent here. If the process is already dead there is no JS to notify; the Step 4 next-launch recovery covers that case.
- [ ] Request `AudioManager.MODE_IN_COMMUNICATION` and `AUDIOFOCUS_GAIN`. Treat any transient/permanent audio-focus loss as `interruption`; stop the native session instead of automatically restarting after another app or phone call releases focus.
- [ ] Make End idempotent: UI End, notification End, task removal, focus loss, and module teardown all converge on one terminal transition and one resource-release pass.

**Verify:** Kotlin tests cover legal state transitions, repeated End, focus loss, task removal, and `START_NOT_STICKY`. Run `cd android; .\gradlew :handsfree-voice:testDebugUnitTest :app:processDebugMainManifest`; the merged manifest contains the exact permissions/service attributes and no `phoneCall` type or boot receiver.

### 3. Implement native recognition, endpointing, and reply speech

**Files:** modify `modules/handsfree-voice/expo-module.config.json`, `modules/handsfree-voice/ios/HandsfreeVoice.podspec`, `modules/handsfree-voice/android/src/main/java/com/versutus/handsfreevoice/HandsfreeCallService.kt`, `modules/handsfree-voice/android/src/main/java/com/versutus/handsfreevoice/HandsfreeVoiceModule.kt`; create `modules/handsfree-voice/android/src/main/java/com/versutus/handsfreevoice/HandsfreeEndpointing.kt`, `modules/handsfree-voice/android/src/test/java/com/versutus/handsfreevoice/HandsfreeEndpointingTest.kt`, `modules/handsfree-voice/ios/HandsfreeVoiceModule.swift`, `modules/handsfree-voice/ios/HandsfreeVoiceAppDelegateSubscriber.swift`, `modules/handsfree-voice/ios/HandsfreeEndpointing.swift`, `modules/handsfree-voice/ios/Tests/HandsfreeEndpointingTests.swift`, `__tests__/handsfree-native-contract-test.ts`.

- [ ] Android: own one main-thread `SpeechRecognizer` per listening turn with partial results. Set complete silence to 900 ms, possibly-complete silence to 600 ms, minimum speech to 300 ms, and a 60-second utterance ceiling. `onResults` emits one trimmed `final`; silence with no words emits `noSpeech`; recoverable recognizer endings restart only while the same user-started session remains active. After emitting a silence-triggered `final`, restart the recognizer immediately rather than on the next JS instruction: the Step 4 grace window can only cancel a pending send in favor of a resumed utterance if the recognizer is already listening again during `confirming`, and restart latency otherwise clips the first syllables of that resumed speech.
- [ ] iOS: configure `AVAudioSession` as `.playAndRecord`, mode `.voiceChat`, options `.defaultToSpeaker` and `.allowBluetooth`, then keep `AVAudioEngine` active for the call’s lifetime. Feed samples to `SFSpeechAudioBufferRecognitionRequest` in `listening` **and in `confirming`** — the grace window is only real if a resumed utterance is still being recognized while the send is held; when the endpointing rule below finishes one recognition task, open the next one immediately for the same reason as the Android restart above. While waiting/speaking/muted, discard samples without recognition or persistence so the audio background mode remains legitimate but ambient audio is not retained. (Corrected 2026-09-12 second review: the previous wording fed samples "only in `listening`", which would have made Step 4's grace window a no-op on iOS — resumed speech during `confirming` would have been discarded, exactly the mid-thought interruption the window exists to prevent.)
- [ ] iOS endpointing: after speech crosses the adaptive noise floor, 900 ms below it finishes the recognition task; 10 seconds with no speech emits `noSpeech`; 60 seconds caps an utterance. Emit the best trimmed transcription once. No audio file is written on either platform.
- [ ] Native TTS: queue the supplied `speechChunks` in order through `TextToSpeech` / `AVSpeechSynthesizer`, applying the existing Bot voice identifier, rate, and pitch. Stop recognition before speech to prevent echo; emit `speechFinished` only after the last chunk. `stopSpeaking` must flush the queue and never emit a stale completion.
- [ ] **Barge-in VAD (seamlessness pass, item 1):** while `speaking`, run a lightweight energy-based voice-activity listener on the same communication-mode audio session already active for echo cancellation — this does not run `SpeechRecognizer`/`SFSpeechRecognizer` and produces no transcript. On Android use an `AudioRecord` tap owned by the service; on iOS, an `AVAudioEngine` input tap computing short-window RMS. (Corrected 2026-09-12 second review: the previous wording also offered "the recognizer's own RMS callback" on Android — that callback (`onRmsChanged`) only fires while a recognition session is active, and this step's own echo rule stops recognition before speech, so during `speaking` there is no recognizer RMS to read.) Voice crossing the ambient noise floor for a continuous ~250 ms emits `bargeIn` exactly once, then the listener stops itself until the next `speaking` phase. On `bargeIn`, immediately flush the TTS queue (the same path `stopSpeaking` uses) and stop — do not wait for `speechFinished`. This does not transcribe the interrupting speech; JS starts a fresh `startListening()` in response, and normal endpointing captures what the user says.
- [ ] **Amplitude event (seamlessness pass, item 4):** during `listening` and `speaking`, emit `level` (0–1) at a modest rate (~10/s) from whichever audio tap is already open for recognition or barge-in detection. This is best-effort UI polish, not a correctness signal — a platform that cannot supply it cheaply may omit it, and JS must render a static (non-animated) state rather than fail when it is absent.
- [ ] **Send earcon playback (seamlessness pass, item 4):** implement `playSendEarcon()` from Step 1's contract natively — Android `SoundPool` (or `MediaPlayer`) and iOS `AVAudioPlayer`, both routed through the call's already-active audio session, playing one short bundled sound file shipped as a module resource (`modules/handsfree-voice/android/src/main/res/raw/`, `modules/handsfree-voice/ios/` resource bundle). This keeps the diff free of any `package.json` audio dependency: the global constraints exclude `expo-audio`, and `expo-speech` cannot play a non-speech asset. Playback is fire-and-forget; a failure to play is silent, never an event or an error into the reducer.
- [ ] Observe `AVAudioSession.interruptionNotification`; on `.began`, emit `interruption` and tear down. The AppDelegate subscriber performs the same idempotent cleanup on termination; register it in `expo-module.config.json` and declare the endpointing XCTest bundle as a CocoaPods `test_spec` in the podspec (a podspec cannot add a target to the app project; `test_spec` is the CocoaPods mechanism for module-owned tests). Route/headset loss is fatal to the call rather than silently switching microphones.

**Verify:** module-level tests/fakes prove one final per turn, empty speech never sends, 900 ms endpointing, 10-second no-speech behavior, 60-second cap, chunk order, stop/late-callback suppression, interruption cleanup, a `bargeIn` fires exactly once per sustained voice-onset during `speaking` and never during `listening`/`waiting`, a `bargeIn` flushes the TTS queue before emitting, and `level` events (when present) never block or delay `final`/`bargeIn`. Build Android with `cd android; .\gradlew :app:assembleDebug`; build iOS in Step 8 because this checkout is on Windows.

### 4. Add the pure call state machine and crash-safe draft recovery

**Files:** create `src/lib/voice/handsfree-session.ts`, `src/lib/voice/handsfree-recovery.ts`, `__tests__/handsfree-session-test.ts`, `__tests__/handsfree-recovery-test.ts`; modify `src/lib/gateway/composer-draft.ts`, `__tests__/composer-draft-test.ts`.

- [ ] Define `HandsfreePhase = 'idle'|'starting'|'listening'|'confirming'|'sending'|'waiting'|'speaking'|'muted'|'ending'|'ended'` and terminal reasons `user|disconnect|system-interruption|app-killed|thread-changed|recognition-failed|send-failed|speech-failed` in the pure reducer `reduceHandsfreeSession`. Every event Step 1 declares has a defined input here, so none is emitted-and-ignored: `partial`/`final` feed `listening`/`confirming`, `noSpeech` leaves the reducer in `listening` (native restarts the recognizer itself; nothing sends), `speechFinished` and `bargeIn` drive `speaking -> listening`, `interruption` ends with `system-interruption`, `endRequested` (the Android notification's End action, Step 2) ends with `user` through exactly the same transition the UI's End takes, `fatalError` ends with `recognition-failed` or `speech-failed` according to the native reason it carries, and `level` never reaches the reducer at all (it is banner-only, Step 7).
- [ ] Lock the cycle: `listening -> confirming` on one non-empty final transcript (never straight to `sending` — see the grace-window bullet below); `confirming -> sending` only once the grace window elapses with no further speech; `sending -> waiting` when the provider observes the correlated assistant placeholder appear in `messages` — NOT on the `sendChatInput` promise resolving: for plain text that promise resolves only after the reply stream completes (`sendMessage` awaits `streamChat`, `gateway-provider.tsx:2214`), so waiting on it would hold the phase at `sending` for the whole reply and Step 6's progressive speech would never start before completion. The promise's eventual resolution is a confirmation only, and the reducer must tolerate it arriving in any later phase; `waiting -> speaking` only on the correlated assistant message (streaming or completed — Step 6 speaks progressively); `speaking -> listening` on native `speechFinished` **or** `bargeIn`. Mute stops recognition but never touches reply playback: from `listening`/`confirming`/`speaking` the phase becomes `muted` and the reducer records the phase it left; a pending grace send from `confirming` is cancelled on mute (the operator muted rather than finished a thought — holding a send they may not know is armed is worse than dropping it, and the recovery layer keeps the words); `speechFinished`/`bargeIn` while `muted` are consumed so playback state stays accurate, and unmute returns to `speaking` if the reply is still playing, otherwise to `listening` — never a silent skip of a queued reply, and never an unmute that opens the mic while the Bot is still talking.
- [ ] **Grace window before send (seamlessness pass, item 2):** on entering `confirming`, start a short timer (default 600 ms — a first pass, expected to need the same on-device tuning as the native silence thresholds; do not treat it as final without the device matrix in Final verification). If native emits a new `partial` or `final` before the timer fires, cancel the pending send, concatenate the new transcript onto the held text (recognizer results in this window are independent utterances chunked by the recognizer's own silence cutoff, not one continuous stream — concatenation is the correct join), restart the timer, and return to `confirming`. Only once the timer elapses with nothing pending does `sendChatInput` fire with the fully accumulated text. This exists because fixed-duration silence endpointing is a documented, specific failure mode of exactly this kind of system — it is what caused OpenAI's original turn-based Advanced Voice Mode to interrupt users mid-thought before GPT-Live replaced it with full-duplex audio. This grace window is the client-side mitigation available without that architecture.
- [ ] Persist the latest partial/final under `handsfree-recovery:<composerDraftKey>` on every transcript event. Clear it only after `sendChatInput` accepts the turn as `sent`. On any terminal path or next launch, merge a stale recovery into the ordinary composer with `spokenDraftText`, then remove the recovery record.
- [ ] Keep recovery text out of `spokenDraftHold`: call transcripts have their own writer and never invoke the B1 hold callbacks. Preserve an existing typed draft byte-for-byte until recovery is actually needed.

**Verify:** Jest covers every transition, duplicate/late native events, empty finals, mute/unmute (including mute during `speaking` preserving the reply and unmute returning to `speaking`), send failure, process-restart recovery, recovery merged after existing draft text, and clearing only after `sent`. Plus: a resumed utterance within the grace window cancels the pending send and concatenates correctly; the timer firing with nothing pending sends exactly the accumulated text; a `bargeIn` during `speaking` transitions to `listening` and never fires the `speechFinished` cleanup twice; `endRequested` reaches `ended` with reason `user` through the identical transition the UI End takes; `noSpeech` in `listening` sends nothing and stays `listening`; a late `sendChatInput` resolution arriving after the phase has already moved on is a no-op; `confirming` is included in every place the reducer's tests assert phase invariants (e.g. no legal path skips it between `listening` and `sending`).

### 5. Make call auto-send connected-only and command-safe

**Files:** modify `src/context/gateway-provider.tsx`; create `src/lib/gateway/chat-input-source.ts`, `__tests__/chat-input-source-test.ts`, `__tests__/handsfree-send-policy-test.ts`.

- [ ] Add `source?: 'composer'|'handsfree-call'` to `sendChatInput` options and add `offline` to `SendChatInputOutcome`. Default/omitted source retains today’s behavior exactly.
- [ ] For `handsfree-call`, require the current captured gateway/session to remain connected and current. If that check fails, return `offline` without calling `queueOfflineInput`; the recovery layer owns the words. Composer, notification reply, run, and queue-flush callers continue to queue/refuse exactly as today.
- [ ] (Added 2026-09-12 second review — without this the user turn silently never renders.) Today's `messageId` option does NOT create a message: `sendMessage(text, existingMessageId)` only clears `queued` on a message already in state (the offline-flush path, where `queueOfflineInput` appended it first, `gateway-provider.tsx:2051-2060` and `:2173-2181`). A call turn passing a fresh `createMessageId('voice-call')` would match nothing and send text that never appears in the transcript. For `source: 'handsfree-call'`, the send path must append the user message itself with the supplied id — `addUserMessage` already accepts an explicit id (`src/lib/gateway/message-reducer.ts:17`) — so the turn renders exactly once and Step 6's reply correlation has a real anchor.
- [ ] (Added 2026-09-12 second review.) Two more existing-behaviour facts the call path must not trip over. First, `sendMessage` early-returns when `isSending` is true (`gateway-provider.tsx:2171`), and `sendChatInput` would still resolve `sent` — for a call that means waiting 120 seconds for a reply to a message that never left. For `handsfree-call`, detect an in-flight send/stream up front and return `busy` instead; the reducer treats `busy` like `send-failed` (terminal, words recovered). Second, for plain text the `sent` resolution arrives only after the reply finishes streaming and does NOT mean the stream succeeded — `sendMessage` catches stream errors onto the message itself (`gateway-provider.tsx:2277+`). Success/failure of a call turn is therefore read off the correlated assistant message's terminal state (Step 6), never off the promise.
- [ ] Treat a call transcript as plain model text even if recognition produced a leading `/`; do not dispatch a slash command from auto-sent speech. Tool calls requested by the model still flow through the existing approval machinery.
- [ ] Do not introduce a backend-name test or a `realtime-voice` capability check. The only server-side prerequisite is the same connected text send path already powering that chat.

**Verify:** targeted Jest proves normal composer offline queueing and slash commands are unchanged, while a call transcript returns `offline` without queueing and a leading slash goes through `sendMessage`. Plus: a call send with a fresh `messageId` appends exactly one visible user message carrying that id, and a call send attempted while another send/stream is in flight returns `busy` rather than a hollow `sent`. Run `npx jest --runInBand __tests__/chat-input-source-test.ts __tests__/handsfree-send-policy-test.ts`.

### 6. Orchestrate calls above navigation and correlate each reply

**Files:** create `src/context/handsfree-voice-provider.tsx`, `src/lib/voice/handsfree-reply.ts`, `__tests__/handsfree-reply-test.ts`, `__tests__/handsfree-provider-contract-test.ts`; modify `src/app/_layout.tsx`, `src/lib/voice/speech.ts`, `__tests__/voice-speech-test.ts`.

- [ ] Mount `HandsfreeVoiceProvider` inside `GatewayProvider` but outside `FontProvider`/navigation (between the two in `src/app/_layout.tsx:561-562`), so route changes, app backgrounding, and the app lock do not unmount the session. It exposes `start`, `mute`, `skipReply`, `end`, current phase/partial, and the captured target. (Corrected 2026-09-12 second review: the previous wording had the provider reading the current surface from `useChatSurface` — but `useChatSurface()` exposes only `{ messages, isSending, isCommandRunning }` (`gateway-provider.tsx:3898-3901`), and which surface is showing is chat-screen-local state, not context (`gateway-provider.tsx:3497-3503`). So: the provider reads `messages`/`isSending`/`isCommandRunning` from `useChatSurface` and connection/gateway/session state from `useGateway`; the call target is handed in by the caller — see the capture bullet below.)
- [ ] On Start: require app state `active`, connected status, no stream/command/approval in progress, a configurable or Bot chat target, and `getAvailability()` reporting recognition, synthesis, and a positive speech-input limit. `startSession` requests mic/speech-recognition plus Android notification permission before starting the foreground service. A denial returns to idle with actionable copy and starts no service.
- [ ] Capture `{ gatewayId, sessionId, surfaceKind, botId?, label, voice }`. The capture is BUILT BY THE CALLER, not derived by the provider: the Step 7 Call control is rendered by `chat-screen.tsx`, which is the one place that already holds the current surface, the `draftThread` (`chat-screen.tsx:441-448`), and the per-Bot `botVoice` (`chat-screen.tsx:562-564`, a `BotVoice` = `{ voiceIdentifier, rate?, pitch? }` from `src/lib/voice/voice-preferences.ts:51-56`) — none of which is readable from any context today. `start(target)` takes that capture. Afterwards the provider watches `activeGateway.id`, `currentSessionId`, and `selectedBotId` from `useGateway`; any change ends the call before another utterance can be sent, and the screen ends it if its own surface diverges from the captured one. This surface restriction follows send semantics, not backend identity; group-room calling stays hidden because it has a different multi-speaker send path.
- [ ] Sends fire on the reducer's `confirming -> sending` transition — once per accumulated turn, after the grace window — NOT "for each final" (a final is what ENTERS `confirming`; sending per final would bypass Step 4's grace window entirely). On that transition: create a user message id with `createMessageId('voice-call')`, persist recovery, call `sendChatInput(text, { source: 'handsfree-call', messageId })` with the fully accumulated text, and remember that id. `handsfreeReplyForTurn` accepts the first later assistant message that belongs after that user id and is not a tool-call/running-command message — it may be **streaming**, not only the completed terminal message (see the progressive-speech bullet below), and per Step 4 it is the placeholder's APPEARANCE in `messages` that moves the reducer to `waiting`, not the send promise. An interrupted/error terminal message or a 120-second reply watchdog ends with `send-failed`; it never retries or speaks a partial answer.
- [ ] (Added 2026-09-12 second review — the phase-to-native wiring was previously implied but written down nowhere, which is how declared events die unconsumed.) The provider performs exactly these native side-effects on reducer transitions: entering `listening` → `startListening()`; `confirming -> sending` → `stopListening()` THEN the send (the recognizer restarted during the grace window must not keep transcribing into the next turn); entering `speaking` → `speak({ chunks, ...voice })`; `speechFinished` or `bargeIn` in `speaking` → `startListening()` (the barge-in TTS flush is native-side, Step 3); entering/leaving `muted` → `setMuted(boolean)`; `skipReply` → `stopSpeaking()` then `startListening()`; native `endRequested` (notification End) → the identical terminal path as UI `end()`; any terminal phase → `stopSession()`, `stopSpeech()` for any B2 queue, recovery promotion, once.
- [ ] **Progressive sentence speech (seamlessness pass, item 3):** reuse B2's existing sentence-boundary chunker (`speechChunks(text, maxLength)`, `src/lib/voice/speech-chunks.ts:106`) against the assistant message's streaming text as it arrives, not only once the message reaches its terminal state. The bound handed to it during a call is the native module's own `maxSpeechInputLength` from `getAvailability()` (Step 1) — the call path does not import `expo-speech` at all. Hand each completed sentence to the native module as soon as it is available; do not wait for the full reply. **Verify before implementing, do not assume:** confirm in `gateway-provider.tsx` that a streaming assistant message is strictly append-only for the lifetime of a call turn (today it is: deltas accumulate through `appendStreamDelta` via the batcher, `src/lib/gateway/stream-batching.ts:108-110` — re-confirm at implementation time). The safeguard for this is a **generic runtime check, never a backend-name branch** (the constraints above forbid that categorically): on every streaming update, assert the already-spoken text prefix is still present unchanged in the message; if a message is ever observed to mutate or shrink at a position already spoken — from any source — that specific turn falls back to waiting for message completion before speaking, and progressive speech resumes normally on the next turn. This is a property of the message, checked live, never a fact about which backend produced it. If the streaming message transitions to interrupted/error mid-speech, stop the TTS queue immediately (same path as `skipReply`) rather than finishing an answer the model retracted. Reopen recognition only on native `speechFinished` or `bargeIn`, once the assistant message has reached a terminal state.
- [ ] `skipReply` flushes native TTS and reopens recognition by explicit tap — this remains the accessible, silent-environment-safe alternative to speaking over the reply; `bargeIn` (Step 3) is the voice-driven one. Call End invokes native stop, normal `stopSpeech()` for any B2 queue, and recovery promotion once.
- [ ] While a call is active, the same assistant message must not be spoken by both `expo-speech` (B2) and the call module. Implement the guard in `src/lib/voice/speech.ts` — the file this step already modifies — as a module-level call-ownership flag (`beginHandsfreeCall()`/`endHandsfreeCall()`) that makes `speakReply` answer `false` while a call owns audio, exactly as a build with no engine does today. `chat-screen.tsx`'s speaker effect (`chat-screen.tsx:656-687`, driven by `speakerAction` in `src/lib/voice/speech-reply.ts`, which only ever speaks completed messages) then needs no conditional and cannot double-speak even if it fires. Do not change the stored speaker toggle; it resumes after the call ends.

**Verify:** Jest/source-contract tests cover start preconditions, destination capture passed in from the screen, one send per completed turn (not per final), the phase-to-native wiring table above (including `stopListening()` before every send and `startListening()` after both `speechFinished` and `bargeIn`), reply correlation, no double TTS while the `speech.ts` guard is set, reopen-after-finish, skip, disconnect, thread change, permission refusal, and cleanup on provider unmount. Plus: progressive speech starts on the first completed sentence of a streaming message rather than waiting for completion; a message that turns interrupted/error mid-speech stops the TTS queue immediately and does not finish an already-queued sentence; a `bargeIn` reopens recognition exactly like `speechFinished` does; a synthetic mutated/shrinking streaming update triggers the per-turn fallback to wait-for-completion without ever inspecting backend identity, and progressive speech resumes on the following turn. During Final verification item 8 (all four backends), note whether any connected backend ever triggers that fallback in practice — as an observation for later tuning, not as grounds to add a branch.

### 7. Build the unmistakably separate call UI without changing push-to-talk

**Files:** create `src/components/chat/handsfree-call-sheet.tsx`, `src/components/voice/handsfree-call-banner.tsx`, `__tests__/handsfree-call-ui-contract-test.ts`; modify `src/components/chat/chat-composer.tsx`, `src/components/chat/chat-screen.tsx`, `src/app/_layout.tsx`.

- [ ] Add a phone-shaped **Call** control beside, not instead of, the existing press-and-hold mic. It is offered only when the provider says Start can succeed, and its tap builds the call target from the screen's own state (surface, `draftThread`, `botVoice`) and hands it to `start(target)` — the provider cannot read those from context (see Step 6). Keep `handleMicPressIn`, `handleMicPressOut`, `spokenDraftHold`, and their accessibility labels/gesture wiring unchanged (`chat-composer.tsx:124-170`, `:430`).
- [ ] First and every explicit Start opens `HandsfreeCallSheet` with: “Hands-free calls listen only after you start them. Speech is sent automatically when you pause. You can interrupt a reply just by talking. The call continues while Versutus is in the background. End it here or from Android’s notification.” Buttons are **Cancel** and **Start call**.
- [ ] While active, disable manual send/dictation for that same thread without clearing its draft and show `HandsfreeCallBanner` as a sibling of the root `Stack` inside the root `View` in `src/app/_layout.tsx` (inside `AppBootstrap`/`AppLockGate`, where context from the provider mounted above `FontProvider` reaches it), so every in-app route retains controls. Display Bot/thread label, `Listening|Sending|Waiting for reply|Speaking|Muted`, live partial text, the permanent **speech auto-sends** label, Mute/Unmute, contextual Skip reply, and destructive End. The internal `confirming` phase (Step 4's grace window) renders as `Listening` — it is an implementation detail, not a state the operator needs a new word for.
- [ ] **Ambient visual (seamlessness pass, item 4):** a small Skia-drawn indicator in the banner animates from the native `level` amplitude stream when present — a simple pulsing shape is enough; this is not the place to build a new design system. When `level` is absent or a platform declines to supply it, render the same shape static rather than fail or show an error. This is polish, never load-bearing: no logic anywhere depends on `level` arriving.
- [ ] **Send earcon (seamlessness pass, item 4):** on the transition into `sending`, call the native `playSendEarcon()` from Step 1/3 — one short, pre-recorded non-verbal sound bundled with the module, played through the call's own audio session, adding no `package.json` dependency — so a slow network/model does not read as the call being dead. This must never be conflated with GPT-Live's verbal backchannel acknowledgments — it is a fixed UI sound, not the model saying anything, and the disclosure sheet/documentation must not imply otherwise.
- [ ] Accessibility: announce phase changes without announcing every partial; label the controls “Start hands-free call”, “Mute hands-free call”, “Skip spoken reply”, and “End hands-free call”. The `Speaking` state's accessibility label includes "say something to interrupt" so the barge-in capability is discoverable without sight. Do not use a wake-word string anywhere.

**Verify:** Jest pins the exact disclosure/copy (including the interrupt-by-speaking line) and proves the mic path still calls only draft writers. Manual foreground smoke proves typed text survives start/end, dictation still leaves text for review, call finals bypass the composer and auto-send once, the ambient indicator degrades to static without `level`, and the earcon plays exactly once per send with no synthesized-speech fallback.

## Call termination and failure behavior

- **Normal End:** UI or Android notification End immediately stops recognition/TTS, deactivates the audio session/foreground service, releases audio focus, promotes unsent recovery to the composer, and never restarts.
- **Gateway disconnect:** end with `disconnect`; never put a final into the offline outbox. Already accepted user turns remain in chat. A reply that completes remotely is loaded normally on reconnect but is not spoken by a dead call.
- **Incoming phone/VoIP call:** iOS audio-session interruption or Android audio-focus loss ends the Versutus call, releases the mic/speaker, and requires a fresh visible Start after the system call. It does not auto-resume listening.
- **App kill/task swipe/OS process death:** Android’s `stopWithTask=true`, `onTaskRemoved`, and `START_NOT_STICKY` remove the service/notification; iOS process death ends its audio session. No boot or relaunch path resumes. The latest JS-delivered partial is recovered into the composer on next launch; audio after that persisted partial is honestly unrecoverable because audio is never recorded.
- **Recognition/TTS fatal error or headset route loss:** stop the call, surface the native reason in the banner on return, and recover unsent text. Recoverable no-speech is not fatal and sends nothing.

### 8. Close documentation only after the native acceptance gate

**Files:** modify `README.md`, `FUTURE-ITEMS.md` only after Final verification items 3–8 pass.

- [ ] Document the two voice contracts, background mic indicator/Android ongoing notification, on-device/platform recognition privacy, text-only backend traffic, interruption behavior, and the absence of wake word/always-listening.
- [ ] Document interrupt-by-speaking and progressive sentence speech precisely as what they are: a short on-device voice-onset detector and sentence-level TTS pipelining, not full-duplex audio and not a realtime provider. Do not use the words "full-duplex" or reference GPT-Live/Gemini Live by name in shipped copy — this is Versutus's own client-side approximation, not a claim of matching them.
- [ ] Move “Hands-free background voice chat” to the shipped ledger only after Android and iOS background loops pass. **B4 is now specifically**: continuous simultaneous listen-while-speaking and verbal backchanneling via a realtime provider — the barge-in and progressive-speech approximations in this plan do not close that gap and B4 must not be marked forfeited-and-done; it remains open future work.

**Verify:** `rg -n "always-listening|auto-send|background|realtime-voice|full-duplex" README.md FUTURE-ITEMS.md` tells the same story as this plan and contains no backend-specific availability claim and no full-duplex/parity claim.

## Final verification

1. Run `npm run verify` and require all config checks, `tsc --noEmit`, lint, Jest with the coverage ratchet, and Gate `node:test` to pass. Gate tests should be unchanged because this route adds no Gate protocol.
2. Run `cd android; .\gradlew :handsfree-voice:testDebugUnitTest :app:assembleDebug`. Inspect `android/app/build/intermediates/merged_manifests/debug/processDebugMainManifest/AndroidManifest.xml` for all five permissions and the `microphone|mediaPlayback`, non-exported, stop-with-task service.
3. Run an iOS preview/dev build on macOS or EAS and inspect the built Info.plist for `UIBackgroundModes` containing `audio`, plus both usage descriptions. A config-only assertion is insufficient.
4. On a physical Android 13+ device and a physical current-iOS device, run three complete turns in each condition: foreground, another app foregrounded, and screen locked for at least ten minutes. Confirm final transcript auto-sends once, each spoken turn appears exactly once as an ordinary user message in the chat transcript, the banner leaves `Listening`/`Sending` for `Waiting for reply`/`Speaking` while the reply is still streaming (proving the placeholder-triggered transition, not the late send-promise resolution), the reply completes, every chunk is spoken, and the mic reopens.
5. On Android, confirm the ongoing notification appears for the whole session, its End action works with Versutus backgrounded — and returning to the app shows the call actually ended (banner gone, reducer terminal, any unsent transcript recovered into the composer), proving the notification End reached JS and was not only a native-side stop — and task swipe removes both service and notification. On iOS, confirm the mic indicator/audio continues only during the user-started session.
6. On both platforms, accept an incoming phone/VoIP call while Versutus is listening and while it is speaking. Versutus must end, release audio immediately, preserve unsent text, and never auto-resume afterward.
7. Kill the app while listening, waiting, and speaking. Relaunch: no call resumes; no text auto-sends; the latest persisted unsent transcript appears in the correct composer; already-sent turns remain ordinary history.
8. Repeat one complete call turn on connected Hermes, OpenCode, Codex, and Claude Code profiles whose advertised chat path is ready. Confirm identical client behavior and no backend-name branches, new capability, credential, binary audio, or audio file in app/Gate logs.
9. Regression-check B1–B3: hold mic -> live transcript in composer -> release -> nothing sent -> edit -> manual Send -> streamed reply -> optional per-Bot `expo-speech` playback. Speaker off still produces no TTS, and a call does not alter the stored speaker preference.
10. **Seamlessness pass, on physical devices:** while the Bot is speaking, say a short phrase — confirm playback stops within roughly the ~250 ms onset window, no transcript from the interruption leaks into the previous turn, and a fresh listening turn captures it correctly. Separately, start a call, speak a sentence, pause for noticeably longer than a normal breath but under the grace window, then continue the thought — confirm exactly one message is sent containing the whole thought, not two. Confirm progressive speech visibly begins before a multi-sentence reply finishes streaming.
11. Read the shipped README/FUTURE-ITEMS copy once more after item 10 passes and confirm it still matches the honest framing above: interrupt-by-speaking and progressive speech are named for what they are, B4 is still listed as open, and nothing reads as full-duplex or as parity with any named competitor product.

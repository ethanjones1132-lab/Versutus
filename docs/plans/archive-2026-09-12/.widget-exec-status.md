# Android widget plan — execution status (never committed)

Worktree: `C:\Users\ethan\.codex\worktrees\bee6\Versutus` · branch `sprint/features-functions-ui`
Plan: `docs/plans/2026-09-12-android-widget-plan.md` · Brief: `docs/plans/EXEC-BRIEF-android-widget-hermes.md`
Base HEAD at start: `8a83907`

## 0.1 — DONE
commit: 7741525
verify: exit 0 (jest + tsc + lint + coverage ratchet green; test:gate 745 pass / 0 fail)
gradle: not run (plan: Kotlin classes arrive in 0.2–0.3; Gradle first runs in 0.2)
notes: contract test failed first with ENOENT on expo-module.config.json exactly as the plan predicts, then passed 4/4 after the module, Gradle build, manifest and resources landed.

## 0.2 — DONE
commit: a641963
verify: exit 0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — `:versutus-widget:testDebugUnitTest`, 8 tests, 0 failures
notes: |
  Two environment setups the plan assumes but this worktree lacked, neither of them a deviation from the plan's steps:
  1. `android/local.properties` was missing, so the first Gradle run died with "SDK location not found".
     Wrote it with the same `sdk.dir` the main repo uses (C:\Users\ethan\AppData\Local\Android\Sdk)
     and exported ANDROID_HOME in the shell, because Task 0.5 Step 1's `prebuild --clean` will delete
     the generated `android/` (and with it local.properties) later.
  2. Confirmed autolinking picks up `modules/versutus-widget` WITHOUT prebuild: `:versutus-widget` configured
     and compiled on the pre-existing generated `android/`, so the plan's prebuild-first fallback was not needed here.
  First Gradle run failed exactly as the plan predicts (Unresolved reference 'WidgetPayload'/'WidgetStamp'),
  then passed 8/8 once the two Kotlin files landed.

## 0.3 — DONE
commit: a206c88
verify: exit 0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — `:versutus-widget:testDebugUnitTest :versutus-widget:compileReleaseKotlin`, 11 tests, 0 failures
notes: |
  Both halves failed first exactly as the plan predicts (jest: ENOENT on VersutusStatusWidget.kt;
  Gradle: Unresolved reference 'WidgetLayout'/'WidgetVariant'), then both passed: jest 6/6, Gradle 11 tests / 0 failures.

  DEVIATION (one line, recorded per the brief's hard rule 7):
  `VersutusWidgetModule.kt` — `clearPayload`'s `?: return@AsyncFunction` does not compile.
  Expo's no-arg overload is `inline fun AsyncFunction(name: String, body: () -> Any?)`
  (node_modules/expo-modules-core/android/.../objects/ObjectDefinitionBuilder.kt:220-228), and Kotlin
  forbids a bare `return` in a lambda whose type is `Any?`:
    e: VersutusWidgetModule.kt:28:48 Return type mismatch: expected 'Any?', actual 'Unit'.
  Smallest change keeping the plan's intent: `?: return@AsyncFunction null` (same early exit, same
  "no react context, do nothing" behaviour; the JS contract for clearPayload is still Promise<void>).
  Verified by `BUILD SUCCESSFUL` after the change. The plan's own Task 0.3 Step 6 anticipated small
  compile fixes of this kind (it pre-authorises a kotlinx-coroutines line and a Glance rc01 fallback);
   neither of those was needed — Glance 1.2.0 resolved and compiled, and `kotlinx.coroutines` resolved
   from the existing toolchain with no extra dependency.

## 0.4 — DONE
commit: 483f914
verify: FINISHED verify EXIT=0 (test:gate 745 pass / 0 fail)
gradle: not run (Task 0.4 is JS/app-side only; Kotlin compiled in 0.2–0.3)
notes: |
  Resumed from Hermes' interrupted partial work exactly as the brief's Resume protocol describes.
  Inspected the uncommitted diff against Task 0.4's steps; all of Steps 1 and 3–6 were already in
  place and correct (payload + test, widget-device routing through modules/versutus-widget,
  app.json enableAndroid:false + Android block removed, package.json expo.autolinking.android.exclude,
  glanceable-widget.android.tsx deleted, widget-target-test pins replaced including the
  androidWidgetPayload import). Ran Step 7: focused jest 46/46 pass, then the full gate.
  No deviations; committed with the plan's exact message.

## 0.5 — PENDING-DEVICE
commit: 9d93b80
verify: FINISHED verify EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — `:versutus-widget:testDebugUnitTest :handsfree-voice:testDebugUnitTest assembleRelease` (11m 13s)
notes: |
  Device-only steps skipped per the brief: no phone is connected and `adb` is forbidden.
  Steps 1–3 all passed without a device:
    - prebuild --clean EXIT=0; the stale `VersutusStatusProvider.kt` is gone.
    - assembleRelease BUILD SUCCESSFUL; recreated `android/local.properties` after the clean, per the brief.
    - merged release manifest names exactly `com.versutus.widget.VersutusStatusReceiver`, nothing for
      `VersutusStatusProvider` or `expo.modules.widgets`.
  Step 4 (adb install) skipped; Step 5 checklist written to
  `docs/plans/2026-09-12-android-widget-device-log.md` with all 10 items PENDING-DEVICE.
  DEVIATION (brief-sanctioned, order-of-work #4): Step 6's commit omits `FUTURE-ITEMS.md` §4
  (it may only change once the checklist passes) and the message says "as pending a device"
  instead of "mark the Android half shipped", because the half is not shipped yet.

## M1 (expansion) — DONE
commit: d510eaa
verify: n/a (docs only)
gradle: not run
notes: M1 rows expanded into docs/plans/2026-09-12-widget-m1.md per order-of-work #2.

## 1.1 — DONE
commit: 3738aa3
verify: FINISHED verify-m1-1 EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest
notes: |
  Resumed from the previous session's uncommitted partial work (WidgetColors.kt new, the M1 pin in
  versutus-widget-module-contract-test.ts, the Build.VERSION.SDK_INT guard in VersutusStatusWidget.kt
  and a build.gradle change). All matched Task 1.1 Steps 1/3/4, so they were kept and finished
  (jest 7/7, Gradle BUILD SUCCESSFUL, verify EXIT=0) and committed with the plan's message.
  DEVIATION (hard rule 7): the commit also stages modules/versutus-widget/android/build.gradle.
  Task 1.1 names only WidgetColors.kt, VersutusStatusWidget.kt and the contract test, but
  `lightColorScheme`/`darkColorScheme` come from androidx.compose.material3, which
  glance-material3:1.2.0 does not put on this module's compile classpath. The smallest change that
  keeps the plan's intent is one `implementation 'androidx.compose.material3:material3:1.0.0'` line
  (the app graph resolves the usual highest version). Without it the new Kotlin does not compile;
  with it Gradle is green.

## 1.2 — DONE
commit: 4e3ea8c
verify: FINISHED verify-m1-2 EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest
notes: |
  Failing-first recorded: `Unresolved reference 'TINY'` from WidgetLayoutTest before the enum grew.
  Then green. Added WidgetVariant.TINY, the `widthDp < 80f && heightDp < 80f` branch, a TINY
  DpSize in sizeMode/companion, the extracted Dot composable, and the lowered minimums in
  versutus_status_widget_info.xml (targetCell 4x2 unchanged). No deviations.

## 1.3 — DONE
commit: b6652a7
verify: FINISHED verify-m1-3 EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :versutus-widget:compileReleaseKotlin
notes: |
  Failing-first recorded in both halves (jest: snapshot.runs undefined, payload.v still 1;
  Gradle: `Unresolved reference 'WidgetRun'`). Then green: snapshot gains an optional
  `runs: GlanceableRun[]` folded from in-flight runs (first prompt line as title, 80-char-agnostic),
  payload goes to v2 with at most three runs, and WidgetPayload.parse accepts 1 and 2 while 3 still
  asks for an update. `WidgetRun` added with a defaulted `runs` so v1 rows parse unchanged.
  No deviations.

## 1.4 — DONE
commit: 907cfa6
verify: FINISHED verify-m1-4 EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :versutus-widget:compileReleaseKotlin
notes: |
  Failing-first recorded (contract test had no `contentDescription`). Then the card builds one
  sentence ("Versutus: <status>. <work>. <stamp>") and attaches it via `.semantics`; text stays in
  sp with no fixed heights. No deviations.

## M1 end — DONE
commit: 8134062
verify: n/a (docs only; the M1-end gate was verify-m1-4 EXIT=0)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :handsfree-voice:testDebugUnitTest assembleRelease (9m 50s)
notes: M1 device acceptance appended to docs/plans/2026-09-12-android-widget-device-log.md as PENDING-DEVICE.

## M2 (expansion) — DONE
commit: 4e32cef
verify: n/a (docs only)
gradle: not run
notes: M2 rows expanded into docs/plans/2026-09-12-widget-m2.md. Task 2.3 is deferred, not expanded.

## 2.1 — DONE
commit: 3163f84
verify: FINISHED verify-m2-1 EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :versutus-widget:compileReleaseKotlin
notes: |
  Failing-first recorded in both halves (jest: snapshot.bots undefined, payload.bots undefined,
  stale provider pins; Gradle: Unresolved reference WidgetBot/WidgetLinks). Then green.
  The fold takes recent Bot Chats (ActivityRun.botId, in run order) first, then roster order,
  deduped and capped at three; an unlabelled id carries itself as its label. The provider reads
  the roster once per connected transition into widgetBots (same shape as routineJobs) and feeds
  the fold; widget-target-test.ts's pinned fold and dependency list were updated with it.
  New Kotlin WidgetLinks.percentEncode is pure and JVM-tested (URLEncoder would have used `+`).
  No deviations.

## 2.2 — DONE
commit: 1374e7a
verify: FINISHED verify-m2-2 EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :versutus-widget:compileReleaseKotlin
notes: |
  Failing-first recorded (no "Decide in Versutus"). Then the approval secondary line became a
  DecideRow that opens versutus://chat; no actionRunCallback or sendBroadcast anywhere in the
  widget, so nothing can decide in-widget (ADR 0001). No deviations.

## 2.3 — DEFERRED
commit: none
verify: not run
gradle: not run
notes: |
  DEFERRED — waits on the voice plan's launch key. The brief's order-of-work #3 says to skip the
  Start voice call button: the voice architecture is being redesigned and the signed launch key
  (HandsfreeLaunchKey.sign) it needs does not exist on this branch. No code written.

## 2.4 — DONE
commit: 9a19818
verify: FINISHED verify-m2-4 EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :versutus-widget:compileReleaseKotlin
notes: |
  Failing-first recorded in both halves (jest: module not found + no payload.redact;
  Gradle: Unresolved reference 'redact'). Then green: a device preference store
  (src/lib/settings/widget-privacy.ts) with a subscriber set, carried through the fold as
  `redact` and emitted in the payload; the payload omits result, runs and bots when redacted and
  the Glance card guards the same lines defensively. The Settings Privacy card gains the switch,
  and the provider reads the preference on mount and on every toggle.
  DEVIATION (cosmetic, hard rule 7): the write effect's fold is kept on one line
  (`glanceableSnapshot({ ..., redact: widgetRedact })`) instead of the expansion's multiline form,
  so the existing source pin in widget-target-test.ts could be a single exact string; the pin and
  its dependency list were updated with it. No behavioural difference.

## M2 end — DONE
commit: 792996f
verify: n/a (docs only; the M2-end gate was verify-m2-4 EXIT=0)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :handsfree-voice:testDebugUnitTest assembleRelease (2m 51s)
notes: M2 device acceptance appended to docs/plans/2026-09-12-android-widget-device-log.md as PENDING-DEVICE.

## M3 (expansion) — DONE
commit: eff9c15
verify: n/a (docs only)
gradle: not run
notes: M3 rows expanded into docs/plans/2026-09-12-widget-m3.md.

## 3.1 — DONE
commit: ffe016d
verify: FINISHED verify-m3-1 EXIT=0 (test:gate 745 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :versutus-widget:compileReleaseKotlin
notes: |
  Failing-first recorded in both halves (jest: no WidgetRefreshPolicy.enqueue; Gradle:
  Unresolved reference WidgetRefreshPolicy). Then green: a CoroutineWorker redraws through
  updateAll, its request uses NetworkType.NOT_REQUIRED (no network constraint so Doze cannot
  starve it), and the module enqueues it KEEP from setPayload. build.gradle adds
  androidx.work:work-runtime-ktx:2.9.1 (present in the local Gradle cache). No deviations.

## 3.3 — DONE
commit: 332f6ff
verify: FINISHED verify-m3-3 EXIT=0 (test:gate 748 pass / 0 fail)
gradle: not run (Gate/JS only)
notes: |
  Failing-first recorded (no widget companion; widgetUpdates undefined). Then green:
  notifications.preferences defaults widgetUpdates to false and accepts it on set, and the
  notifier appends a data-only companion message `{ to, data: { kind: 'widget', widget } }` only
  to rows with widgetUpdates === true. The companion is a valid v2 payload (status connected,
  work "No runs in flight", result = the event text, writtenAt = now), so WidgetPayload.parse
  accepts it. No deviations.

## 3.2 — DONE
commit: 4b25223
verify: FINISHED verify-m3-2 EXIT=0 (test:gate 748 pass / 0 fail)
gradle: not run at task level; covered by the M3-end assembleRelease
notes: |
  Failing-first recorded (module not found). Then green: expo-task-manager ~57.0.17 added with
  `npx expo install`; widget-push-task.ts defines the `versutus-widget-push` task, reads
  `data.widget` (a Gate-stamped v2 payload) and writes it through the Glance module, and
  registerWidgetPushTask calls Notifications.registerTaskAsync on Android. syncPushRegistration
  registers it before the token question, swallowing a failed registration. No deviations.

## M3 end — DONE
commit: 1ae0b2b
verify: n/a (docs only; the M3-end gate was verify-m3-2 EXIT=0)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :handsfree-voice:testDebugUnitTest assembleRelease (3m 58s)
notes: M3 device acceptance appended to the device log as PENDING-DEVICE. expo-task-manager autolinked with no prebuild.

## M4 (expansion) — DONE
commit: 731a763
verify: n/a (docs only)
gradle: not run
notes: M4 expanded into docs/plans/2026-09-12-widget-m4.md as one task (4.1).

## 4.1 — DONE
commit: 407865b
verify: FINISHED verify-m4-1 EXIT=0 (test:gate 748 pass / 0 fail)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :versutus-widget:compileReleaseKotlin
notes: |
  Failing-first recorded in both halves (jest: no configure activity / widgetFeatures;
  Gradle: Unresolved reference WidgetConfigState). Then green. WidgetConfigureActivity pins a Bot
  per instance through updateAppWidgetState(context, PreferencesGlanceStateDefinition, glanceId)
  and redraws; the card reads the pin with currentState<Preferences>() and narrows the Bot rows to
  it (falling back to the pinned id's own row when the payload no longer carries it); the provider
  info gains widgetFeatures and the receiver names the activity, which is declared for
  APPWIDGET_CONFIGURE.
  DEVIATION (hard rule 7): the expansion's configure body assigned `prefs.remove(...)`/`prefs[...]`
  directly. With the explicit `PreferencesGlanceStateDefinition` argument the API's lambda receives
  an immutable `Preferences` and must return the next value, so the implemented body converts with
  `prefs.toMutablePreferences()` (a member function, not an import) and returns it. Same state
  written, same early-cancel/finish behaviour; only the call shape changed, and the plan's named
  API (`updateAppWidgetState`, `PreferencesGlanceStateDefinition`) is still exactly what is called.

## M4 end — DONE
commit: 9085efa
verify: n/a (docs only; the M4-end gate was verify-m4-1 EXIT=0)
gradle: BUILD SUCCESSFUL — :versutus-widget:testDebugUnitTest :handsfree-voice:testDebugUnitTest assembleRelease (3m 41s)
notes: M4 device acceptance appended to the device log as PENDING-DEVICE.

## M5 (expansion) — DONE
commit: 2604146
verify: n/a (docs only)
gradle: not run
notes: M5 expanded into docs/plans/2026-09-12-widget-m5.md as one task (5.1). No Android code.

## 5.1 — DONE
commit: 70e7ed0
verify: FINISHED verify-m5 EXIT=0 (test:gate 748 pass / 0 fail)
gradle: not run (iOS/JS only; no Android code per the plan)
notes: |
  Failing-first recorded (no widgetURL pin). Then the iOS root VStack gains
  `widgetURL('versutus://chat')`, the same deep link the Android card uses.
  Verification on EAS/macOS is PENDING-MACOS per the brief (no Mac here).

# Execution summary

Phase 0 (0.1–0.4) was already DONE/PENDING-DEVICE at resume; Task 0.4's interrupted partial work
was finished and committed. All milestones that can run without a device or a Mac have run:

- M1: 1.1 brand fallback colours, 1.2 1x1 tile, 1.3 large run list (payload v2), 1.4 accessibility.
- M2: 2.1 Bot quick-launch, 2.2 Decide, 2.4 privacy switch. 2.3 DEFERRED (voice plan's launch key).
- M3: 3.1 six-hourly WorkManager roll-over, 3.2 expo-task-manager data-only push, 3.3 Gate companion.
- M4: 4.1 per-instance Bot pin via the configure activity.
- M5: 5.1 iOS tap URL.

Final gate: `npm run verify` EXIT=0 (test:gate 748 pass / 0 fail), `:versutus-widget:testDebugUnitTest`
and `assembleRelease` BUILD SUCCESSFUL. Every device check and the iOS EAS check remain
PENDING-DEVICE / PENDING-MACOS; `FUTURE-ITEMS.md` §4 was left unchanged, as the brief requires.

WIDGET PLAN EXECUTION COMPLETE

// Shared chat screen (components/chat/chat-screen.tsx) — platform split kept
// only as a thin wrapper so Android/iOS/web cannot diverge again.
export { ChatScreen as default } from '@/components/chat/chat-screen';

// The web/plain build of the ambient call indicator: Metro resolves the
// `.native.tsx` sibling on device and this plain shape everywhere else.

export {
  HandsfreeCallIndicatorFallback as HandsfreeCallIndicator,
  type HandsfreeCallIndicatorProps,
} from './handsfree-call-indicator-fallback';

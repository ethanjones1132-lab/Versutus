import { GatewayCardInner, type GatewayCardInnerProps } from '@/components/gateway-card-inner';

export function GatewayCard(props: GatewayCardInnerProps) {
  return <GatewayCardInner {...props} />;
}